{
  "name": "json-server",
  "version": "1.0.0",
  "description": "use json-server",
  "main": "index.js",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "node index.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "thanh phung",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}